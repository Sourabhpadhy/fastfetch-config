# #######################################################################################
# AUTOGENERATED HYPRLAND CONFIG.
# EDIT THIS CONFIG ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################



# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hypr.land/Configuring/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf

################
### MONITORS ###
################

# See https://wiki.hypr.land/Configuring/Monitors/
monitor =, preferred, auto, 1


###################
### MY PROGRAMS ###
###################

# See https://wiki.hypr.land/Configuring/Keywords/

# Set programs that you use
$terminal = kitty
$fileManager = nautilus
$menu = hyprlauncher


#################
### AUTOSTART ###
#################

exec-once = ~/.local/bin/matugen-watch.sh

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

# exec-once = $terminal
# exec-once = nm-applet &
# exec-once = waybar & hyprpaper & firefox

# --- Status Bar & Wallpaper ---
#exec-once = waybar & hyprpaper

# --- Notifications ---
#exec-once = swaync

# --- Network & Bluetooth ---
exec-once = nm-applet --indicator
exec-once = blueman-applet

# --- Clipboard History ---
exec-once = wl-paste --type text --watch cliphist store
exec-once = wl-paste --type image --watch cliphist store

# --- Password Prompt (KDE or gnome Agent) ---
#exec-once = /usr/lib/polkit-kde-authentication-agent-1
exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1



#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hypr.land/Configuring/Environment-variables/

env = QT_QPA_PLATFORMTHEME,qt6ct
env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24


###################
### PERMISSIONS ###
###################

# See https://wiki.hypr.land/Configuring/Permissions/
# Please note permission changes here require a Hyprland restart and are not applied on-the-fly
# for security reasons

# ecosystem {
#   enforce_permissions = 1
# }

# permission = /usr/(bin|local/bin)/grim, screencopy, allow
# permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow


#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hypr.land/Configuring/Variables/

# https://wiki.hypr.land/Configuring/Variables/#general
general {
    gaps_in = 5
    gaps_out = 20
    border_size = 2
    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)
    resize_on_border = false
    allow_tearing = false
    
    # Set your layout here once!
    layout = scrolling
}

scrolling {
    column_width = 0.85
    direction = right
    focus_fit_method = 0
    follow_focus = true
    fullscreen_on_one_column = false
    explicit_column_widths = 0.333, 0.5, 0.667, 1.0
}


# https://wiki.hypr.land/Configuring/Variables/#decoration
decoration {
    rounding = 10
    rounding_power = 2

    # Change transparency of focused and unfocused windows
    active_opacity = 0.85
    inactive_opacity = 0.75

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hypr.land/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 6
        passes = 3
        new_optimizations = true
        vibrancy = 0.1696
    }
}


# https://wiki.hypr.land/Configuring/Variables/#animations
animations {
    enabled = yes, please :)

    # Default curves, see https://wiki.hypr.land/Configuring/Animations/#curves
    #        NAME,           X0,   Y0,   X1,   Y1
    bezier = easeOutQuint,   0.23, 1,    0.32, 1
    bezier = easeInOutCubic, 0.65, 0.05, 0.36, 1
    bezier = linear,         0,    0,    1,    1
    bezier = almostLinear,   0.7,  0.6,  0.75, 1.05
    bezier = quick,          0.15, 0,    0.1,  1
    bezier = myBezier,       0.05, 0.9,  0.1,  1.05


    # Default animations, see https://wiki.hypr.land/Configuring/Animations/
    #           NAME,          ONOFF, SPEED, CURVE,        [STYLE]
    animation = global,        1,     10,    default
    animation = border,        1,     5.39,  easeOutQuint
    animation = windows,       1,     4.79,  easeOutQuint
    animation = windowsIn,     1,     4.1,   easeOutQuint, popin 87%
    animation = windowsOut,    1,     1.49,  linear,       popin 87%
    animation = fadeIn,        1,     1.73,  almostLinear
    animation = fadeOut,       1,     1.46,  almostLinear
    animation = fade,          1,     3.03,  quick
    animation = layers,        1,     3.81,  easeOutQuint
    animation = layersIn,      1,     4,     easeOutQuint, fade
    animation = layersOut,     1,     1.5,   linear,       fade
    animation = fadeLayersIn,  1,     1.79,  almostLinear
    animation = fadeLayersOut, 1,     1.39,  almostLinear
    animation = workspaces,    1,     1.94,  almostLinear, slide
    animation = workspaces,    1,     6,     myBezier    , slide
    animation = workspacesIn,  1,     4,     almostLinear, slide
    animation = workspacesOut, 1,     4,     almostLinear, slide
    animation = zoomFactor,    1,     7,     quick
}

# Ref https://wiki.hypr.land/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrule {
#     name = no-gaps-wtv1
#     match:float = false
#     match:workspace = w[tv1]
#
#     border_size = 0
#     rounding = 0
# }
#
# windowrule {
#     name = no-gaps-f1
#     match:float = false
#     match:workspace = f[1]
#
#     border_size = 0
#     rounding = 0
# }

# See https://wiki.hypr.land/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hypr.land/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hypr.land/Configuring/Variables/#misc
misc {
    force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = false # If true disables the random hyprland logo / anime girl background. :(
}


#############
### INPUT ###
#############

# https://wiki.hypr.land/Configuring/Variables/#input
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = false
    }
}

# See https://wiki.hypr.land/Configuring/Gestures
gesture = 3, horizontal, workspace

# Example per-device config
# See https://wiki.hypr.land/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


###################
### KEYBINDINGS ###
###################

# See https://wiki.hypr.land/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier


# Copy (Ctrl+C) on Side Button 1
bind = , mouse:276, exec, wtype -M ctrl c -m ctrl

# Paste (Ctrl+V) on Side Button 2
bind = , mouse:275, exec, wtype -M ctrl v -m ctrl


# --- Screenshots ---

# 1. Take a screenshot of the selected area and save it to your Pictures folder
bind = SUPER, S, exec, grim -g "$(slurp)" ~/Pictures/screenshot_$(date +'%s_grim.png')

# 2. Take a screenshot of the selected area and copy it to the clipboard
bind = SUPER, mouse:275, exec, grim -g "$(slurp)" - | wl-copy

# 3. Take a screenshot of the entire screen and save it
bind = , Print, exec, grim ~/Pictures/screenshot_$(date +'%s_grim.png')

# 4. Take a screenshot of the entire screen and copy it to the clipboard
bind = SHIFT, Print, exec, grim - | wl-copy


# Switch between tabs in the group
bind = $mainMod, Tab, changegroupactive, f  # Forward
bind = $mainMod Shift, Tab, changegroupactive, b # Backward


# Example binds, see https://wiki.hypr.land/Configuring/Binds/ for more
bind = $mainMod, Q, exec, $terminal
bind = $mainMod, C, killactive,
bind = $mainMod, M, exec, command -v hyprshutdown >/dev/null 2>&1 && hyprshutdown || hyprctl dispatch exit
bind = $mainMod, E, exec, nautilus
bind = $mainMod, V, togglefloating,
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, J, togglesplit, # dwindle

# --- Scrolling Layout Keybindings ---

bind = $mainMod, period, layoutmsg, move +col                                  # Move focus to the next column (right/down)
bind = $mainMod, comma, layoutmsg, move -col                                   # Move focus to the previous column (left/up)
bind = $mainMod SHIFT, period, layoutmsg, swapcol r                            # Swap current column with the one to the right
bind = $mainMod SHIFT, comma, layoutmsg, swapcol l                             # Swap current column with the one to the left
bind = $mainMod, bracketright, layoutmsg, colresize +conf                      # Snap column width to the next larger preset
bind = $mainMod, bracketleft, layoutmsg, colresize -conf                       # Snap column width to the next smaller preset
bind = $mainMod, F, layoutmsg, fit active                                      # Center and fit the active column to the screen
bind = $mainMod SHIFT, H, exec, hyprctl keyword scrolling:direction right      # Change scroll direction to horizontal
bind = $mainMod SHIFT, V, exec, hyprctl keyword scrolling:direction down       # Change scroll direction to vertical

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Switch workspaces with mainMod + [0-9]l
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10


# Example special workspace (scratchpad)
bind = $mainMod, T, togglespecialworkspace, magic
bind = $mainMod SHIFT, T, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hypr.land/Configuring/Window-Rules/ for more
# See https://wiki.hypr.land/Configuring/Workspace-Rules/ for workspace rules

# Example windowrules that are useful

windowrule {
    # Ignore maximize requests from all apps. You'll probably like this.
    name = suppress-maximize-events
    match:class = .*

    suppress_event = maximize
}

windowrule {
    # Fix some dragging issues with XWayland
    name = fix-xwayland-drags
    match:class = ^$
    match:title = ^$
    match:xwayland = true
    match:float = true
    match:fullscreen = false
    match:pin = false

    no_focus = true
}

# Hyprland-run windowrule
windowrule {
    name = move-hyprland-run

    match:class = hyprland-run

    move = 20 monitor_h-120
    float = yes
}

# GLava Rules
#windowrulev2 = float, class:^(GLava)$
#windowrulev2 = pin, class:^(GLava)$
#windowrulev2 = noborder, class:^(GLava)$
#windowrulev2 = nofocus, class:^(GLava)$
#windowrulev2 = size 100% 100%, class:^(GLava)$
#windowrulev2 = move 0 0, class:^(GLava)$
#windowrulev2 = noinitialfocus, class:^(GLava)$




# -----------------------------------------------------
# TABBED GROUPS & FLOATING (Dynamic Colors)
# -----------------------------------------------------

# 1. Keybinds
# Super+V = Make window float (drag it anywhere)
#bind = $mainMod, V, togglefloating

# Super+G = Make window a "Group" (Tabbed mode)
#bind = $mainMod, G, togglegroup

# Super+Tab = Switch between tabs
#bind = $mainMod, Tab, changegroupactive, f
#bind = $mainMod Shift, Tab, changegroupactive, b

# 2. Styling the Tabs (Using Dynamic Variables)

#group {
    # Active Tab Border (Tokyo Night Blue/Purple)
    # We use explicit colors here to prevent crashes
    #col.border_active = rgba(7aa2f7ff) rgba(bb9af7ff) 45deg
    
    # Inactive Tab Border
    #col.border_inactive = rgba(565f89cc) rgba(565f89cc) 45deg

    # Locked Group
    #col.border_locked_active = rgba(f7768eff) rgba(bb9af7ff) 45deg
    #col.border_locked_inactive = rgba(565f89cc)

    #groupbar {
        # Text color
        #text_color = rgba(c0caf5ff)
        
        # Active Tab Bar (Bright Blue)
        #col.active = rgba(7aa2f7cc)
        
        # Inactive Tab Bar (Dark Background)
        #col.inactive = rgba(1a1b26cc)
        
        # Locked Tab Bar
        #col.locked_active = rgba(f7768ecc)
        #col.locked_inactive = rgba(1a1b26cc)
  #  }
# }
